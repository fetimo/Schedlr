Legend: 

_/ = completed
#_ = to be done
-- = scrapped

_/ Get store of events to display in list
_/ Sort store into sections of site
_/ Implement storing each chosen event in localStorage (push to array?)
_/ Find a more elegant way to reload the page without using 'reload();'
_/ Use ext. setup or launch: not both! So sort out inconsistencies there
_/ Optimise
-- Add tab bar for sorting by different categories ('category' or 'type')
-- Instead of refreshing app, simply flip between calendar and list view?
-- Make calendar view
_/ Change text to icons on tab bar
-- Add recommended events feature
12_ Sort alphabetically